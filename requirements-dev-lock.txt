# Locked development dependencies for reproducible CI/dev environments.
# Regenerate with: pip freeze --exclude-editable > requirements-dev-lock.txt
# (run inside a clean virtualenv after `pip install -e ".[dev]"`)
pytest>=7.4.0
pytest-cov>=4.1.0
mypy>=1.5.0
ruff>=0.1.0
black>=23.9.0
pip-audit>=2.7.0
